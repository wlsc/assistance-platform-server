---
- name: MISC | Install last versions of Bash, OpenSSl other libs
  apt: 
    name={{ item }} 
    update_cache=yes 
    state=latest
  with_items:
    - bash
    - openssl
    - libssl-dev
    - libssl-doc
  tags: 
    [install, first]